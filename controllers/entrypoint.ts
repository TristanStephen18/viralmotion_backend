import path from "path";

export const entry = path.join(
  process.cwd(),
  "./remotion_templates/TemplateHolder/src/index.ts"
);


export const entry2 = path.join(
  process.cwd(), "remotion_templates/TemplateHolder/src/index.ts"
)

export const distentry = path.join(
  process.cwd(), "dist/remotion_templates/TemplateHolder/src/index.ts"
)
